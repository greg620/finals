<h2>Ajouter un tutoriel</h2>

<% if @tutoriel.errors.any? %>
    <div class="notif error">
      <% @tutoriel.errors.full_messages.each do |message| %>
        <div><%= message %></div>
      <% end %>
    </div>
<% end %>


<%= render 'form', tutoriel: @tutoriel, url: new_admin_tutoriel_path %>